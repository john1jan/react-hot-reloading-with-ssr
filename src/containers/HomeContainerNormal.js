const React = require('react');
const SampleNormal = require('../components/SampleNormal');

var HomeContainerNormal = React.createClass({

    render: function () {
        return (
            <SampleNormal />
        );
    }
});

module.exports = HomeContainerNormal;
